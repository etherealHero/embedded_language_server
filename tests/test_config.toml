# https://www.connectionstrings.com/sql-server/
# https://learn.microsoft.com/en-us/dotnet/framework/data/adonet/connection-strings
ado_connection_string = "Server=myServerAddress;Database=myDataBase;User Id=myUsername;Password=myPassword;"

# sql query that returns dataset with columns:
# - 'Identifier' string, not null, unique, must use
# - 'HoverInfo' string, nullable, optional
# - 'DefinitionInfo' string, nullable, optional
# - 'DefinitionFileExtension' string, nullable, optional
get_symbols_query = """
select
    'Lorem' as Identifier,
    'Lorem ipsum dolor est' as HoverInfo,
    'Lorem Ipsum is a standard dummy text used in printing and web design' as DefinitionInfo,
    '.md' as DefinitionFileExtension
union all
select
    'Ipsum' as Identifier,
    'Ipsum dolor est' as HoverInfo,
    'Ipsum is a standard dummy text used in printing and web design' as DefinitionInfo,
    '.md' as DefinitionFileExtension
"""

# injection protection for get_symbols_query generated by 'sign <CONFIG_FILE>' command
# you should manual sign config file after change get_symbols_query option via cli app (see --help)
sign = "628cced7ef8102e6d3cefe6a4d333e3c03177d69317d9eb8bc7a3554d5e2fa44"

# optional properties:
trust_cert = true # if set, the server certificate will not be validated and it is accepted as-is (dafault false)
case_sensitive = false # search Identifier with case sensitive in every DefinitionInfo or not (default true)
